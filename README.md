# Northstar Software Company Landing Page

A responsive software company landing page built with React and Create React App conventions. The active app lives in `frontend/`.

## Run locally

```bash
cd frontend
npm install
npm start
```

The page includes a responsive navigation menu, hero dashboard preview, customer logos, feature cards, testimonial, and call-to-action section.